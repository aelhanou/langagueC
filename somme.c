#include <stdio.h>

int main()
{
    int S;
    int N;
    int t[N];
    printf("entre dimension de tableau : ");
    scanf("%d",N);
    while (t[N])
    {
        scanf("%d",t);
        S = t[N];
        printf("la somme est :%d",S);
    }
    return (0);
}
